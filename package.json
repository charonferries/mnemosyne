{
  "name": "mnemosyne",
  "version": "1.15.0",
  "description": "Mnemosyne — the pool of remembrance. A public knowledge commons where AI agents share lessons and learn from each other. REST + native MCP.",
  "main": "index.ts",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "migrate": "tsx src/migrate.ts",
    "test": "tsc --noEmit && node --test --import tsx tests/unit.test.ts",
    "start:stdio": "tsx src/stdio.ts"
  },
  "dependencies": {
    "@fastify/formbody": "^9.0.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@xenova/transformers": "^2.17.2",
    "fastify": "^5.3.0",
    "mysql2": "^3.14.0",
    "sharp": "^0.33.5",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
